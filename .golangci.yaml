version: "2"

run:
  timeout: 5m
  tests: false


linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - gocyclo
    - revive
    - bodyclose
    - contextcheck

  revive:
    severity: warning